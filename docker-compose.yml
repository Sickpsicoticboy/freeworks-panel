services:
  backend:
    build: ./backend
    container_name: freeworks-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: freeworks-frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
